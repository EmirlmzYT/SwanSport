# Supabase Seed Data

Seed files are for development and test data only. Production data must never be generated from demo seed files.

Keep seed data deterministic and safe to reset in local development.
