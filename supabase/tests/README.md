# Supabase Tests

RLS tests are required because UI hiding is not security. Tests should verify that users can only access data allowed by their role, organization, and guardian relationship context.
