# Supabase Edge Functions

Future Edge Functions will live here. Service role keys and other server-only secrets must remain in Supabase function secrets or deployment environment variables, never in Flutter code.
