name: swansport_app
description: SwanSport MVP Flutter application shell.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  swansport_branch_engine: ^0.1.0
  swansport_core: ^0.1.0
  swansport_design_system: ^0.1.0
  swansport_models: ^0.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
